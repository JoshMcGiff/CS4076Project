#ifndef ROOM_HPP
#define ROOM_HPP

#include <string>

using namespace std;

class Room {
private:
    /* data */
    string roomName;
    //Directions (Locked/Unlocked, can go, etc)
    // Items in the room
    // 


public:

    Room(/* args */);
    ~Room();
};

#endif
